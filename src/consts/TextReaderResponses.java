package src.consts;

public enum TextReaderResponses {
    OK,
    STATEMENT_NOT_FOUND,
    NOTHING_AFTER_THEN,
    THEN_NOT_FOUND,
    NOTHING_AFTER_ELSE,
    DOUBLE_ELSE,
    EXTRA_FI,
    ENDING_NOT_PARSED
}
