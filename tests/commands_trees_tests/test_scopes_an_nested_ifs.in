int x := 30;
if x then
    int y := x + 1;
    if (y > 30) then
        str z := "OK";
    else
        str z := "BAD";
    fi
else
    int z := 999;
    if (x + z) % 3 != 0 then
        str y := "BAD";
    else
        str y := "OK";
    fi
fi
int z := 1;
int y := z;
